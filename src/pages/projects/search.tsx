import { Form, Input } from "antd";
import React, { FC } from "react";

const XSearch:FC = () => {
    return <Form>
        <Form.Item>
            <Input placeholder="项目名" />
        </Form.Item>
    </Form>
}

export default XSearch